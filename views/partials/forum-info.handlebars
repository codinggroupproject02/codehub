<article>
  <hr>
  <div class="title">
    <h3>{{title}}</h3>
  </div>
  <div>
    {{content}}

    <span> -- {{comments.length}} comment(s)</span>
  </div>
  <br>
  <button type='submit' id="commentBtn" onclick="commentButtonAction({{id}})">Comment</button>
  <hr>
</article>